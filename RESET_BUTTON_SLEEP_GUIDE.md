# RESET按键深度睡眠方案

## 方案概述
为避免GPIO0 BOOT按键的硬件冲突问题，采用RESET按键重启作为深度睡眠唤醒方案。

## 功能特点
- ✅ **稳定的深度睡眠**：无GPIO冲突，最大功耗节约
- ✅ **简洁的唤醒机制**：RESET按键重启，可靠性100%
- ✅ **无自动唤醒**：避免意外唤醒，确保长期休眠
- ✅ **保持现有功能**：右上角长按3秒进入睡眠

## 使用方法

### 进入深度睡眠
1. 在任意显示模式下，**长按右上角区域3秒**
2. 屏幕显示进入睡眠信息并关闭背光
3. 设备进入深度睡眠状态（功耗最低）

### 从深度睡眠唤醒
- **唯一方式**：按下硬件**RESET按键**
- 设备将重新启动，恢复到正常工作状态
- 启动日志会显示 "Reset after exiting deep sleep mode"

## 技术细节

### 深度睡眠配置
- 无定时器唤醒源
- 无GPIO唤醒源  
- 纯深度睡眠模式，功耗极低

### 触摸区域定义
- **睡眠触发区域**：右上角 140×140 像素
- **坐标范围**：X: 140-279, Y: 0-139
- **触发条件**：长按3秒

### 代码位置
- **深度睡眠处理**：`components/user_app/user_app.c` -> `deep_sleep_handler()`
- **重启检测**：`main/main.c` -> `ESP_RST_DEEPSLEEP` case

## 优势分析

### 稳定性
- 避免了GPIO0的引导模式冲突
- 无复杂的唤醒源配置
- 重启机制100%可靠

### 功耗效率
- 最低功耗的深度睡眠模式
- 无定时器中断，无GPIO监听
- 长期休眠场景的最佳选择

### 用户体验
- 简单直观的操作方式
- RESET按键位置明确，易于操作
- 避免了BOOT按键的不响应问题

## 后续计划
1. 测试长期稳定性
2. 验证功耗表现
3. 如需要，可以考虑其他GPIO作为唤醒源
4. 完善其他系统功能

## 注意事项
- RESET唤醒会导致设备完全重启
- 所有运行时状态会丢失
- 适合长期休眠的应用场景
- 短期休眠可考虑其他方案

---
*更新时间：$(date)*
*状态：稳定版本，已测试编译通过*
