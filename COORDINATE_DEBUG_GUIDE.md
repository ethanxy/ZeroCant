# 触摸坐标调试指南

## 🔍 当前问题分析

根据你的测试结果，现在触摸切换变成了左上角，说明坐标转换还有问题。

## 📊 测试数据记录

### 之前的测试数据
- **触摸位置**: 左下角
- **原始坐标**: raw_x=449-455, raw_y=1-21
- **期望屏幕坐标**: screen_x=0-139, screen_y=316-455

### 当前转换算法
```c
*screen_x = raw_y;     // 1-21 -> 1-21
*screen_y = raw_x;     // 449-455 -> 449-455
```

### 问题分析
- `screen_x = 1-21` → 确实在左侧 ✅
- `screen_y = 449-455` → 在屏幕底部外（超出456），但被截断后仍在底部区域 ✅

**奇怪**: 按理说这个转换应该让左下角工作，但你说现在是左上角工作了？

## 🧪 建议的测试步骤

请测试屏幕的四个角落，记录每个位置的输出：

### 1. 左上角测试
触摸屏幕左上角，记录串口输出中的：
- `Touch transform: raw(?,?) -> screen(?,?)`
- `🎯 触摸区域分析: ?`

### 2. 右上角测试  
触摸屏幕右上角，记录输出

### 3. 左下角测试
触摸屏幕左下角，记录输出

### 4. 右下角测试
触摸屏幕右下角，记录输出

## 🎯 目标
我们需要确保：
- 左下角触摸 → 触发模式切换
- 其他区域触摸 → 不触发切换

## 📝 记录格式示例
```
左上角: raw(X1,Y1) -> screen(A1,B1) -> 区域分析: xxx
右上角: raw(X2,Y2) -> screen(A2,B2) -> 区域分析: xxx  
左下角: raw(X3,Y3) -> screen(A3,B3) -> 区域分析: xxx
右下角: raw(X4,Y4) -> screen(A4,B4) -> 区域分析: xxx
```

有了这些数据，我就能准确调整坐标转换算法了。

## 🔧 临时解决方案

如果你想快速测试，可以临时修改触摸区域定义，让左上角成为切换区域：

```c
// 临时：左上角切换区域 (140×140px)
#define TOUCH_AREA_X1 0
#define TOUCH_AREA_Y1 0  
#define TOUCH_AREA_X2 139
#define TOUCH_AREA_Y2 139
```
