# 弹道计算组件改进计划

## py-ballisticcalc 功能分析与对比

### 当前ESP32实现状态 ✅
- [x] RK4数值积分引擎
- [x] G1, G7, G8 弹道系数模型
- [x] 基础风效应建模
- [x] 大气密度计算
- [x] 轨迹点计算和存储
- [x] 归零角度查找算法

### py-ballisticcalc具有但我们缺少的功能 ❌

#### 1. 扩展弹道系数模型
- [ ] **G2模型**: 适用于尖头弹
- [ ] **G5模型**: 适用于低阻尖头弹  
- [ ] **G6模型**: 适用于平头弹
- [ ] **GI模型**: 适用于不规则形状弹丸
- [ ] **自定义拖拽函数**: 基于多普勒雷达数据

#### 2. 多BC模型支持 (DragModelMultiBC)
```python
# py-ballisticcalc支持速度相关的多BC
dm = DragModelMultiBC([
    BCPoint(mach=0.0, BC=0.25),
    BCPoint(mach=1.0, BC=0.23), 
    BCPoint(mach=1.5, BC=0.22)
])
```

#### 3. 温度敏感性建模
```python
# 粉末温度敏感性
ammo.calc_powder_sens(Velocity.MPS(820), Temperature.Celsius(0))
muzzle_vel = ammo.get_velocity_for_temp(current_temp)
```

#### 4. 自旋漂移效应
- [ ] **弹丸自旋效应**: 基于膛线缠度和弹丸长度
- [ ] **科里奥利效应**: 地球自转影响
- [ ] **埃特沃什效应**: 水平发射时的重力变化

#### 5. 高级大气建模
- [ ] **海拔相关大气变化**: 更精确的密度模型
- [ ] **标准大气模型**: 国际标准大气(ISA)
- [ ] **湿度影响**: 水蒸气对空气密度的影响

#### 6. 弹道学高级功能
- [ ] **危险空间计算**: danger_space()函数
- [ ] **瞄准角度查找**: look angle支持
- [ ] **弹道表生成**: 完整的射程卡功能
- [ ] **命中概率**: 考虑各种误差源

### 实施优先级

#### 阶段1: 核心弹道模型扩展 (高优先级)
1. **多BC模型实现** - 速度相关BC插值
2. **G2, G5, G6, GI拖拽表** - 完整拖拽模型库
3. **温度敏感性** - 粉末温度影响建模

#### 阶段2: 物理效应完善 (中优先级)  
1. **自旋漂移计算** - 弹丸自旋效应
2. **精确大气模型** - ISA标准大气
3. **湿度影响** - 空气密度修正

#### 阶段3: 高级功能 (低优先级)
1. **危险空间计算** - 目标高度相关
2. **科里奥利/埃特沃什效应** - 地球物理影响
3. **完整弹道表** - 射程卡生成

### 数学公式对比

#### 当前我们的加速度计算:
```c
accel->x = -drag_magnitude * relative_vel.x;
accel->y = gravity_y - drag_magnitude * relative_vel.y;
accel->z = -drag_magnitude * relative_vel.z;
```

#### py-ballisticcalc的完整公式应包括:
1. **拖拽加速度**: `a_drag = -0.5 * ρ * Cd * A * v² / m`
2. **重力加速度**: `a_gravity = g * (1 - altitude_factor)`
3. **科里奥利加速度**: `a_coriolis = 2 * ω × v`
4. **自旋漂移**: `a_spin = Magnus_force / m`

### 内存和性能考虑

#### ESP32限制:
- **RAM**: ~500KB可用，需要优化数据结构
- **Flash**: 充足，可以存储完整拖拽表
- **计算能力**: 240MHz，可以处理复杂计算

#### 优化策略:
1. **拖拽表压缩**: 使用查找表+插值
2. **浮点优化**: 合理使用单精度浮点
3. **内存池**: 预分配轨迹点存储
4. **惰性计算**: 按需计算高级效应

## 下一步行动
1. 实现多BC模型插值算法
2. 添加G2, G5, G6, GI拖拽表数据
3. 实现温度敏感性计算
4. 测试性能和精度对比
