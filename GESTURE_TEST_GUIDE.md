# 手势控制测试指南

## 测试目标
验证角度显示界面和设置界面的手势控制是否按预期工作。

## 预期行为

### 角度显示界面 (angle_display)
✅ **允许操作:**
- **向下滑动** → 显示设置界面

❌ **禁止操作:**
- 向上滑动 → 无效果
- 向左滑动 → 无效果 
- 向右滑动 → 无效果
- 单击、双击、长按 → 无效果

### 设置界面 (settings)
✅ **允许操作:**
- **向上滑动** → 关闭设置界面
- 点击滑块 → 调节亮度
- 点击校准按钮 → 执行校准

❌ **禁止操作:**
- 向下滑动 → 无效果
- 向左滑动 → 无效果
- 向右滑动 → 无效果

## 测试步骤

### 1. 编译并烧录
```bash
cd /Users/xuanyuliu/164
idf.py build
idf.py flash monitor
```

### 2. 测试角度显示界面
1. 确认当前在角度显示界面（显示角度数据）
2. **向下滑动** → 应该呼出设置界面
3. 返回角度显示界面，测试以下手势应该**无效果**：
   - 向上滑动
   - 向左滑动  
   - 向右滑动
   - 单击屏幕
   - 双击屏幕
   - 长按屏幕

### 3. 测试设置界面
1. 从角度显示界面向下滑动呼出设置界面
2. **向上滑动** → 应该关闭设置界面
3. 重新呼出设置界面，测试以下手势应该**无效果**：
   - 向下滑动
   - 向左滑动
   - 向右滑动

### 4. 测试设置界面控件
1. 呼出设置界面
2. 拖动亮度滑块 → 应该能调节屏幕亮度
3. 点击校准按钮 → 应该执行IMU校准
4. 确认这些操作不会意外关闭设置界面

## 调试信息
开启串口监视器查看日志输出：
- `angle_display` 标签：角度显示界面的手势检测日志
- `settings` 标签：设置界面的手势检测日志

### 关键日志示例
```
I (xxxxx) angle_display: Down swipe detected! (distance=XX) - Showing settings...
I (xxxxx) angle_display: Horizontal swipe ignored! dx=XX, dy=XX
I (xxxxx) settings: Settings up swipe detected! (distance=XX) - Hiding settings...
I (xxxxx) settings: Settings horizontal swipe ignored! dx=XX, dy=XX
```

## 手势检测参数
- **最小滑动距离**: 50像素
- **最大滑动时间**: 1000ms
- **方向判断**: 主方向距离必须大于次要方向距离的2倍

## 故障排除

### 手势无响应
1. 检查滑动距离是否足够（>50像素）
2. 检查滑动速度是否合适（<1秒）
3. 检查滑动方向是否正确

### 误触发
1. 检查串口日志中的手势检测信息
2. 确认滑动的主方向和次要方向比例
3. 调整手势参数如有必要

### 设置界面无法关闭
1. 确认在设置界面空白区域滑动，而不是在控件上
2. 确认向上滑动的距离和方向
3. 检查是否有透明手势捕获区域工作正常

## 成功标准
- [x] 角度显示界面只能通过向下滑动呼出设置
- [x] 设置界面只能通过向上滑动关闭  
- [x] 所有其他手势方向被完全忽略
- [x] 设置界面的功能控件正常工作
- [x] 手势检测可靠且响应迅速
- [x] 无意外的界面切换或关闭
