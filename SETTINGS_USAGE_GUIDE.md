# 设置组件使用指南

## 概述

本项目新增了一个设置组件，为ESP-IDF + LVGL项目提供了设备设置功能。用户可以通过友好的触摸界面调节屏幕亮度和校准加速度计。

## 功能特性

### 1. 屏幕亮度调节
- **范围**：10-255 (4%-100%)
- **控制方式**：滑块拖拽
- **实时显示**：当前亮度百分比
- **硬件接口**：使用现有的SH8601 SPI命令控制

### 2. 加速度计校准
- **传感器**：QMI8658C
- **校准过程**：一键触发，自动完成
- **用户反馈**：进度指示器 + 完成提示
- **校准要求**：设备需保持水平静止

### 3. 用户界面
- **现代化设计**：半透明背景，圆角边框
- **触摸友好**：大按钮，易于操作
- **响应式布局**：自适应屏幕尺寸
- **多语言支持**：中文界面

## 操作方法

### 打开设置界面
在主界面（角度显示界面）有两种方式打开设置：

**方式1：长按**（推荐）
1. **长按**屏幕任意位置约1秒钟
2. 设置界面将以覆盖层形式显示

**方式2：双击**（备用方案）
1. 在500ms内**快速双击**屏幕任意位置
2. 设置界面将以覆盖层形式显示

> **注意**：如果长按功能不工作，请使用双击方式。两种方式都会在串口日志中显示详细的调试信息。

### 调节屏幕亮度
1. 在设置界面中找到"亮度"滑块
2. 拖拽滑块调节亮度（10%-100%）
3. 实时查看亮度变化效果
4. 亮度数值显示在滑块上方

### 校准加速度计
1. 将设备平放在水平面上
2. 点击"校准加速度计"按钮
3. 保持设备静止不动
4. 等待校准完成提示（约2-3秒）

### 关闭设置界面
- 点击"关闭"按钮
- 或者再次长按主界面

## 技术实现

### 架构设计
```
主界面 (angle_display)
    ↓ 长按事件
设置组件 (settings)
    ├── 亮度控制 → setBrightnes() → SH8601 SPI
    └── 加速度计校准 → qmi8658_on_demand_cali() → QMI8658C I2C
```

### 内存管理
- 使用`memory_diag`组件进行智能内存分配
- 优先使用PSRAM，降级到DMA内存
- 初始化时显示内存状态
- 支持内存类型检测和诊断

### 事件处理
- **短按**：`LV_EVENT_CLICKED` → 切换显示模式
- **长按**：`LV_EVENT_LONG_PRESSED` → 显示/隐藏设置
- **滑块**：`LV_EVENT_VALUE_CHANGED` → 实时调节亮度
- **按钮**：`LV_EVENT_CLICKED` → 执行对应功能

### 错误处理
- 所有UI对象创建都有错误检查
- 资源分配失败时自动清理
- 空指针保护和边界检查
- 详细的日志输出用于调试

## 扩展能力

### 预留扩展空间
设置组件采用模块化设计，便于添加新功能：

1. **新设置项**
   ```c
   // 添加新的UI元素
   lv_obj_t* new_setting = lv_btn_create(settings_state.container);
   // 配置样式和事件
   ```

2. **数据持久化**
   ```c
   // 集成NVS存储
   #include "nvs_flash.h"
   void settings_save_to_nvs(void);
   void settings_load_from_nvs(void);
   ```

3. **主题切换**
   ```c
   void settings_set_theme(theme_t theme);
   ```

4. **高级校准**
   ```c
   void settings_calibrate_gyroscope(void);
   void settings_calibrate_magnetometer(void);
   ```

### 建议的扩展功能
- WiFi配置界面
- 系统信息显示
- 日志级别设置
- 自动休眠配置
- 姿态显示参数调节
- 数据记录开关

## 故障排除

### 常见问题

1. **设置界面不显示**
   - 检查是否正确长按（持续1秒以上）
   - 确认设置组件已正确初始化
   - 查看日志输出确认事件触发

2. **亮度调节无效**
   - 确认SH8601驱动正常工作
   - 检查SPI通信是否正常
   - 验证`setBrightnes()`函数可用性

3. **加速度计校准失败**
   - 确保设备完全静止
   - 检查QMI8658C I2C通信
   - 查看校准过程日志输出

4. **UI显示异常**
   - 检查LVGL初始化是否完成
   - 确认内存分配是否成功
   - 验证显示屏驱动正常

### 调试方法
```c
// 启用详细日志
ESP_LOGI("settings", "Setting component status");

// 检查内存状态
memory_diag_print_status("settings");

// 验证UI对象
ESP_LOGI("settings", "Container: %p", settings_get_container());
```

## 总结

设置组件为项目提供了完整的设备配置功能，具有良好的用户体验和扩展性。通过合理的架构设计和错误处理，确保了系统的稳定性和可维护性。

主要优势：
- ✅ 直观的触摸操作界面
- ✅ 实时的亮度调节效果
- ✅ 可靠的加速度计校准
- ✅ 完善的错误处理机制
- ✅ 良好的扩展能力
- ✅ 详细的调试支持
